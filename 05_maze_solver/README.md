# Maze Solver

Find the shortest route through a maze using breadth-first search. `#` is a wall, `S` is the start, and `E` is the exit.

Extensions: let users draw a maze, count visited cells, or compare BFS with depth-first search.
